class Direccion {
    constructor(id, direccion, instrucciones_direccion){
        this.id = id;
        this.direccion = direccion;
        this.instrucciones_direccion = instrucciones_direccion;
    }
};

export default Direccion;